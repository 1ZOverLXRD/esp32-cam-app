# ESP32-CAM Optimization — sdkconfig overrides
# Applied on top of sdkconfig; run `idf.py reconfigure` to apply

# --- CPU & Compiler ---
CONFIG_COMPILER_OPTIMIZATION_PERF=y
CONFIG_COMPILER_OPTIMIZATION_ASSERTIONS_DISABLE=y

# --- PSRAM (8MB Octal @80MHz) ---
CONFIG_SPIRAM_FETCH_INSTRUCTIONS=y
CONFIG_SPIRAM_RODATA=y
CONFIG_SPIRAM_SPEED_80M=y
CONFIG_SPIRAM_MALLOC_ALWAYSINTERNAL=0
CONFIG_SPIRAM_TRY_ALLOCATE_WIFI_LWIP=y

# --- Data Cache ---
CONFIG_ESP32S3_DATA_CACHE_64KB=y
CONFIG_ESP32S3_DATA_CACHE_LINE_64B=y

# --- Flash (QIO if supported by hardware) ---
# WARNING: QIO requires flash chip IO2/IO3 connected to module.
# If board doesn't boot after flashing, erase and rebuild with DIO.
CONFIG_ESPTOOLPY_FLASHMODE_QIO=y

# --- WiFi (AP mode, 4 clients max) ---
CONFIG_ESP_WIFI_DYNAMIC_RX_BUFFER_NUM=16
CONFIG_ESP_WIFI_CACHE_TX_BUFFER_NUM=16
CONFIG_ESP_WIFI_SOFTAP_BEACON_MAX_LEN=256
CONFIG_ESP_WIFI_TX_BA_WIN=6

# --- LVGL (v8.4.0) ---
CONFIG_LV_CONF_SKIP=y